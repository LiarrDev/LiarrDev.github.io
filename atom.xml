<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://LiarrDev.github.io</id>
    <title>Liarr&apos;s Studio</title>
    <updated>2021-06-22T15:31:37.499Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://LiarrDev.github.io"/>
    <link rel="self" href="https://LiarrDev.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://LiarrDev.github.io/images/avatar.png</logo>
    <icon>https://LiarrDev.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Liarr&apos;s Studio</rights>
    <entry>
        <title type="html"><![CDATA[『Android Studio』如何导入 SO 库]]></title>
        <id>https://LiarrDev.github.io/post/How-to-Import-SO-in-Android-Studio/</id>
        <link href="https://LiarrDev.github.io/post/How-to-Import-SO-in-Android-Studio/">
        </link>
        <updated>2021-06-07T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>之前的文章『<a href="https://liarrdev.github.io/post/How-to-Import-JAR-in-Android-Studio/">「Android Studio」如何导入 JAR 包</a>』介绍了传统 JAR 包的导入方式，其实 Android 开发中还有一种相对来说用得比较少但却依然十分重要的库类型：SO 文件。</p>
<h1 id="什么是-so-库">什么是 SO 库</h1>
<p>SO 文件（Shared Object）是 Linux 下共享库文件，它的文件格式被称为 ELF 文件格式。由于 Android 操作系统的底层基于 Linux 系统，所以 SO 文件可以运行在 Android 平台上。</p>
<p>一般来说，SO 文件就类似于常说的动态链接库（DLL，即 Dynamic Link Library）, 都是 C 或 C++ 编译出来的。</p>
<p>Android 系统也同样开放了 C/C++ 接口供开发者开发 Native 程序。由于基于虚拟机的编程语言 Java 更容易被人反编译，因此越来越多的应用将其中的核心代码以 C/C++ 为编程语言，并且以 SO 文件的形式供上层 Java 代码调用，以保证安全性。</p>
<h1 id="android-abi">Android ABI</h1>
<p>不同于 Java 利用虚拟机实现「Compile Once, Run AnyWhere」的跨平台的特性，C/C++ 是「Write Once, Compile EveryWhere」的，这就导致了其需要针对不同的平台进行编译。</p>
<p>由于 Android 的开源，不同的厂家可以根据自己的需求在不同的硬件上接入 AOSP，因此不同的 Android 设备使用不同的 CPU，而不同的 CPU 支持不同的指令集。CPU 与指令集的每种组合都有专属的 ABI（Application Binary Interface，应用二进制接口）。</p>
<p>目前常见的 ABI 有如下几种：</p>
<ul>
<li><code>armeabi</code>：第 5 代 ARM v5TE，使用软件浮点运算，兼容所有 ARM 设备，通用性强，速度慢（只支持 <code>armeabi</code>）。</li>
<li><code>armeabi-v7a</code>：第 7 代 ARM v7，使用硬件浮点运算，具有高级扩展功能（支持 <code>armeabi</code> 和 <code>armeabi-v7a</code>）。</li>
<li><code>arm64-v8a</code>：第 8 代，64 位，包含 AArch32、AArch64 两个执行状态对应 32-bit 和 64-bit（支持 <code>armeabi-v7a</code>、<code>armeabi</code> 和 <code>arm64-v8a</code>）。</li>
<li><code>x86</code>：Intel 32 位，一般用于平板（支持 <code>x86</code> 和 <code>armeabi</code> 但性能有所损耗）。</li>
<li><code>x86_64</code>：Intel 64 位，一般用于平板（支持 <code>x86</code> 和 <code>x86_64</code>）。</li>
<li><code>mips</code>：32 位，支持 RISC。</li>
<li><code>mips64</code>：64 位，支持 RISC。</li>
</ul>
<h1 id="项目导入-so-库">项目导入 SO 库</h1>
<p>因为 SO 库是针对不同 CPU 架构编译出来的，所以其往往不会像 JAR 一样只会编译一份，而是会针对不同的 ABI 生成多个 SO 库，所以我们在使用时也需要往项目中导入多个 SO 文件。</p>
<p>由于包含 SO 文件的开源框架并不十分常见，我在这里就以 <a href="https://dev.vivo.com.cn/documentCenter/doc/6">vivo 网游联运 SDK</a> 为例进行介绍。</p>
<p>首先将其提供的 SDK 压缩文件下载到本地，并对其解压，得到如下目录：</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1622991030410.png" alt="vivo 网游联运 SDK 目录" loading="lazy"></figure>
<p>可以看到，<a href="https://dev.vivo.com.cn/documentCenter/doc/6">vivo 网游联运 SDK</a> 提供了 <code>armeabi</code>、<code>armeabi-v7a</code>、<code>arm64-v8a</code>、<code>x86</code> 和 <code>x86_64</code> 共五种架构的支持，SO 文件分别以同名的方式位于这五个文件夹下，这与我们的 <code>res</code> 类型同名文件存放方式是相似的。</p>
<p>接下来就可以导入了，『Android Studio』中 SO 库的导入有三种方式。</p>
<h2 id="通过-libs-目录导入">通过 <code>libs</code> 目录导入</h2>
<p>理论上，『Android Studio』中所有的库文件都可以扔进「libs」目录，正如之前介绍过的『<a href="https://liarrdev.github.io/post/How-to-Import-JAR-in-Android-Studio/">「Android Studio」如何导入 JAR 包</a>』一般。</p>
<p>将 SO 文件及上层文件夹复制到 Module 的「libs」目录下：</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1622991089932.png" alt="通过 libs 目录导入 - Project 视图" loading="lazy"></figure>
<p>由于 Java 代码调用 SO 库涉及到跨语言交互，所以必须通过 JNI（Java Native Interface）进行，同时，通过 JNI 交互的文件也必须通过标识才能被『Android Studio』处理，因此还需要在 Module 的「build.gradle」中加入如下代码：</p>
<pre><code class="language-Groovy">android {
    ...
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }
}
</code></pre>
<p>同步之后即可在项目中使用了。</p>
<p>切换至「Android」视图下，你还可以发现『Android Studio』将其识别为「jniLibs」文件夹：</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1622991108616.png" alt="通过 libs 目录导入 - Android 视图" loading="lazy"></figure>
<h2 id="通过-jnilibs-目录导入">通过 <code>jniLibs</code> 目录导入</h2>
<p>除了将「libs」目录指定为「jniLibs」文件夹，我们还可以为 SO 库创建专门的「jniLibs」目录。</p>
<p>在 <code>/src/main</code> 目录下新建一个名为「jniLibs」的文件夹，将 SO 文件及上层文件夹复制到该目录即可：</p>
<figure data-type="image" tabindex="4"><img src="https://LiarrDev.github.io/post-images/1622991130503.png" alt="通过 jniLibs 目录导入" loading="lazy"></figure>
<p>由于文件夹的名称已经做了标识，所以我们无需添加代码告知『Android Studio』去处理。</p>
<h2 id="以-jar-包的方式导入">以 JAR 包的方式导入</h2>
<p>这种方式比较少见。</p>
<p>我们可以将 SO 库按照如下目录结构压缩成 JAR，之前『<a href="https://liarrdev.github.io/post/How-to-Import-JAR-in-Android-Studio/">「Android Studio」如何导入 JAR 包</a>』一文也提到，JAR 是以 ZIP 格式构建的，所以可以使用任何压缩工具甚至是命令来压缩。</p>
<figure data-type="image" tabindex="5"><img src="https://LiarrDev.github.io/post-images/1622991157376.png" alt="以 JAR 包的方式导入" loading="lazy"></figure>
<p>然后按照『<a href="https://liarrdev.github.io/post/How-to-Import-JAR-in-Android-Studio/">「Android Studio」如何导入 JAR 包</a>』一文所提到的方法，跟导入普通 JAR 包一样导入即可。</p>
<h1 id="当不同库兼容的-abi-不一致">当不同库兼容的 ABI 不一致</h1>
<p>由于系统会根据 CPU 架构去运行不同的 SO 库，在实际开发项目中，假如我们引用了其他第三库，而这个库可能兼容的 ABI 与已有的不一致，就会产生问题。</p>
<p>举个例子，比如上方举例的 <a href="https://dev.vivo.com.cn/documentCenter/doc/6">vivo 网游联运 SDK</a> 兼容了 <code>armeabi</code>、<code>armeabi-v7a</code>、<code>arm64-v8a</code>、<code>x86</code> 和 <code>x86_64</code> 五种架构，我现在引入<a href="https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html">微信语音识别 SDK</a>，发现其支持 <code>armeabi-v7a</code>、<code>armeabi</code>、<code>mips</code> 和 <code>x86</code> 四种架构，导入后每个文件夹中的 SO 文件并不对等，运行到某些设备上就会崩溃。</p>
<figure data-type="image" tabindex="6"><img src="https://LiarrDev.github.io/post-images/1622991336026.png" alt="导入两个支持不同 ABI 的 SDK" loading="lazy"></figure>
<p>因为当应用运行时，设备会有先查看与 CPU 匹配的最优 ABI，如果没有，才会去查找其他 ABI，当 CPU 与 ABI 匹配上后，应用仅会读取该 ABI 下的 SO 库，其他 ABI 会直接被无视，假若该 ABI 缺失了对应的 SO 库，运行到对应代码时就会崩溃。</p>
<p>所以，遇到这种情况时，我们需要对 ABI 做处理。</p>
<h2 id="仅保留交集-abi">仅保留交集 ABI</h2>
<p>这种解决方法非常简单粗暴，就是取交集，仅保留所有库都支持的 ABI，其他文件夹都删除。</p>
<p>比如上方举例提到的两个第三方 SDK，取交集后可以保留 <code>armeabi</code>、<code>armeabi-v7a</code> 和 <code>x86</code> 三种 ABI。</p>
<figure data-type="image" tabindex="7"><img src="https://LiarrDev.github.io/post-images/1622991383844.png" alt="两个支持不同 ABI 的 SDK 取并集" loading="lazy"></figure>
<p>当然，考虑到市面上设备占比以及目标用户分布，你甚至可以仅保留 <code>armeabi-v7a</code> 一种 ABI。</p>
<h2 id="将已有的-so-文件复制到其他-abi-目录">将已有的 SO 文件复制到其他 ABI 目录</h2>
<p>前面提到，<code>arm64-v8a</code> 是兼容 <code>armeabi-v7a</code> 和 <code>armeabi</code> 的，所以，你还可以将其复制到空缺的文件夹。</p>
<p>比如将<a href="https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html">微信语音识别 SDK</a> 中 <code>armeabi</code> 下的 SO 文件复制到 <code>arm64-v8a</code> 下，就可以使其支持 <code>arm64-v8a</code> 了。</p>
<h2 id="abi-filters">ABI Filters</h2>
<p>部分第三方库你还可能是通过远程依赖的，这时候对不同 SDK 的 ABI 处理就会变得复杂，但我们其实可以通过 Gradle 来操作。</p>
<p>在 Module 的「build.gradle」中加入如下代码：</p>
<pre><code class="language-Groovy">android {
    ...
    defaultConfig {
        ...
        ndk {
            abiFilters 'armeabi', 'arm64-v8a', 'x86'
        }
    }
}
</code></pre>
<p>这样，『Android Studio』就可以针对我们指定的 ABI 进行构建了。</p>
<h1 id="参考内容">参考内容</h1>
<ul>
<li><a href="https://developer.android.google.cn/ndk/guides/abis?hl=en">Android ABIs | Android NDK | Android Developers</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Windows 修改 Hosts]]></title>
        <id>https://LiarrDev.github.io/post/How-to-Edit-Hosts-in-Windows/</id>
        <link href="https://LiarrDev.github.io/post/How-to-Edit-Hosts-in-Windows/">
        </link>
        <updated>2021-05-31T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>之前『<a href="https://liarrdev.github.io/post/A-Brief-Overview-of-Hosts/">一文带你简单了解 Hosts</a>』科普了 Hosts 相关内容，这次就教大家如何修改 Hosts。</p>
<p>考虑到大多数用户最常用的都是 Windows 系统，所以就先来介绍 Windows 下修改 Hosts 的方法，别担心，其他系统后面有机会也会介绍。</p>
<p>要修改 Hosts，那首先得找到 Hosts 文件。大多数情况下，Hosts 文件的路径为：<code>C:\Windows\System32\drivers\etc\hosts</code>。</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1624293899560.png" alt="Hosts File" loading="lazy"></figure>
<p>Hosts 文件可以用任何文本编辑工具打开，如记事本等。</p>
<p>Windows 平台下原始的 Hosts 文件内容如下：</p>
<pre><code class="language-Hosts"># Copyright (c) 1993-2009 Microsoft Corp.
#
# This is a sample HOSTS file used by Microsoft TCP/IP for Windows.
#
# This file contains the mappings of IP addresses to host names. Each
# entry should be kept on an individual line. The IP address should
# be placed in the first column followed by the corresponding host name.
# The IP address and the host name should be separated by at least one
# space.
#
# Additionally, comments (such as these) may be inserted on individual
# lines or following the machine name denoted by a '#' symbol.
#
# For example:
#
#      102.54.94.97     rhino.acme.com          # source server
#       38.25.63.10     x.acme.com              # x client host
#
# localhost name resolution is handled within DNS itself.
# 127.0.0.1       localhost
# ::1             localhost
</code></pre>
<p>如果你怀疑你的 Hosts 文件被病毒或木马修改，可复制以上内容修复 Hosts 文件。</p>
<p>值得一提的是，每行 <code>#</code> 后面的内容都是注释，所以实际上清空 Hosts 内容对系统正常运行并没有什么影响。</p>
<p>Hosts 一般由网站的管理员提供，当然也有一些特殊途径，Hosts 的获取不在本文的介绍范围，一些公共的网站 IP 你可以尝试使用『<a href="https://www.ipaddress.com">IPAddress.com</a>』等第三方工具查询。</p>
<p>当你需要添加 Hosts 的时候只需要在文件的末尾追加即可，Hosts 由 IP 和域名共同组成，如：</p>
<pre><code class="language-Hosts">172.217.8.249    developer.android.com
</code></pre>
<p>修改完成后保存即可。</p>
<p>有时候你会遇到无法直接修改系统 Hosts 文件的情况，可以通过将 Hosts 文件复制到桌面，修改完成后再覆盖来解决。</p>
<p>还有可能会出现不能覆盖的情况，这可能涉及到权限问题，应检查当前登录账号的权限，以及文件的读写权限等。</p>
<p>需要注意，Hosts 文件配置的映射是静态的，如果网络上的计算机更改了请及时更新 IP 地址，否则将不能访问。</p>
<p>最后，相信有些朋友会遇到修改 Hosts 后不生效的情况，一般是由于 DNS 缓存的原因导致的。</p>
<p>系统为了加快用户打开网站的速度，在首次访问网站成功之后，会把 DNS 解析的结果暂时性地保存在本地缓存（称为 DNS 缓存）里一小段时间，如果浏览器在这段时间里再次打开同一个网址，则会自动从 DNS 缓存里取出结果，而不会请求远程的 DNS 服务器，也不会查询 Hosts 文件，从而节省时间提高打开速度。</p>
<p>因此如果遇到修改了 Hosts 文件但并不生效的情况，就刷新一下系统的 DNS 缓存即可。</p>
<p>Windows 平台只需打开任一命令行终端，如『CMD』或『Windows PowerShell』等，执行命令：</p>
<pre><code class="language-Bash">➜   ipconfig /flushdns
</code></pre>
<p>可以看到返回“已成功刷新 DNS 解析缓存”，代表刷新成功，如果还是不行，就重启大法好吧。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[『Android Studio』如何导入 JAR 包]]></title>
        <id>https://LiarrDev.github.io/post/How-to-Import-JAR-in-Android-Studio/</id>
        <link href="https://LiarrDev.github.io/post/How-to-Import-JAR-in-Android-Studio/">
        </link>
        <updated>2021-05-26T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>开发中我们常常会需要用到第三方库来实现某些功能，可以避免重复造轮子，提高开发效率。</p>
<p>在 Android 中，JAR 包可以说是一代元老了，因为在 2017 Google IO 大会以前，Java 作为唯一的 Android 原生开发语言，以及在『Eclipse』的统治下，JAR 包是最通用的第三方库流通方式。</p>
<p>JAR 是一种归档文件，以 ZIP 格式构建，全称是 Java Archive。与 ZIP 不同的是，JAR 不仅用于压缩和发布，而且还用于部署和封装库、组件和插件程序，并可被像编译器和 JVM 这样的工具直接使用。在 JAR 中包含特殊的文件，如 Manifest 和部署描述符，用来指示工具如何处理特定的 JAR。</p>
<p>正是由于过去十几年『Eclipse』的影响，在『Android Studio』中导入 JAR 包也非常简单。</p>
<p>首先，当然要将 JAR 文件放到项目中，置于 Module 的「libs」目录下，我这里以开源库 <code>Gson</code> 为例。</p>
<p>在「Android」视图下，「libs」目录是默认不显示的：</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1620490548383.png" alt="" loading="lazy"></figure>
<p>我们无需新建该文件夹，只需切至「Project」视图即可：</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1620490590290.png" alt="" loading="lazy"></figure>
<p>接着是添加依赖，将 JAR 复制到「libs」目录后，鼠标对该 JAR 包右键菜单，点击「Add As Library...」：</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1620490610733.png" alt="" loading="lazy"></figure>
<p>并选择导入的 Module：</p>
<figure data-type="image" tabindex="4"><img src="https://LiarrDev.github.io/post-images/1620490628873.png" alt="" loading="lazy"></figure>
<p>确定后，Gradle 会自动帮我们同步依赖。</p>
<p>你可以看到 Module 的「build.gradle」文件中自动添加了对应的内容：</p>
<pre><code class="language-Groovy">dependencies {
    ...
    implementation files('libs/gson-2.8.6.jar')
}
</code></pre>
<p>当你有多个 JAR 包需要导入时，其实并不需要对每个 JAR 包都进行上述操作，你可以使用一行代码导入该 Module 下所有 JAR 包：</p>
<pre><code class="language-Groovy">dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    ...
}
</code></pre>
<p>这行代码的意思就是将「libs」目录下所有的 JAR 文件添加依赖，只需在添加 JAR 包后同步 Gradle 即可。</p>
<p>你可能会想，那直接用第二种方法不就好了吗，多方便是不是？</p>
<p>这就要考虑 Module 的依赖传递关系了，可以参考『<a href="https://liarrdev.github.io/post/Android-Gradle-Plugin-Dependency-Configurations/">Android Gradle Plugin 依赖项配置</a>』一文，当有多个 JAR 包需要导入，但需要使用不同的依赖项配置时，就需要分别导入。</p>
<p>所以应当根据合适的场景使用合适的导入方式。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android Gradle Plugin 依赖项配置]]></title>
        <id>https://LiarrDev.github.io/post/Android-Gradle-Plugin-Dependency-Configurations/</id>
        <link href="https://LiarrDev.github.io/post/Android-Gradle-Plugin-Dependency-Configurations/">
        </link>
        <updated>2021-05-16T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>做 Android 开发必定会依赖第三方库，比如常用的如网络请求框架 <a href="https://liarrdev.github.io/post/Enjoy-OkHttp-in-One-Shot/"><code>OkHttp</code></a>、图片加载框架 <a href="https://liarrdev.github.io/post/Enjoy-Glide-in-One-Shot/"><code>Glide</code></a> 等，即使你完全采用原生 API 来开发，也会依赖 <code>AppCompat</code> 或者 <code>Support</code> 之类的官方库。</p>
<p>以前 Android 开发是在『Eclipse』上进行的，开发者使用第三方库往往需要手动下载 Jar 包再到『Eclipse』内引用，在『Android Studio』推出后，得益于 Gradle 构建工具，我们只需在 Gradle 文件中用使用一行代码即可将第三方库从远程站点依赖到项目中，十分方便。</p>
<p>我们引用依赖一般都是使用 <code>implementation</code> 配置依赖项，的确大多数第三方库文档的 Usage 都是这么写的，大多数情况下我们这么用也没问题，但是我们依然需要了解其他的依赖项配置。</p>
<h1 id="implementation">implementation</h1>
<p>首先我们从最熟悉的 <code>implementation</code> 开始，为什么 <code>implementation</code> 是绝大多数第三方库文档所介绍的配置方式，同时也是『Android Studio』的默认配置方式？</p>
<p>原因其实很简单，与其他配置方式相比，此依赖项配置可以显著缩短构建时间，因为这样可以减少构建系统需要重新编译的 Module 数。</p>
<p>当 <code>implementation</code> 依赖项更改了其 API，Gradle 只会重新编译该依赖项以及直接依赖于它的 Module。同时，该 Module 在编译时不会将该依赖项泄露给其他 Module，也就是说，其他 Module 只有在运行时才能使用该依赖项。</p>
<h1 id="api">api</h1>
<p>当一个 Module 包含 <code>api</code> 依赖项时，该 Module 会以传递方式将该依赖项导出到其他 Module，以便这些 Module 在运行时和编译时都可以使用该依赖项。</p>
<p>此配置的行为类似于 <code>compile</code>（现已弃用），会以传递方式导出到其他上游消费者。<br>
其与 <code>implementation</code> 的最大区别在于能否被上游消费者调用，如下图所示：</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1620490674516.jpeg" alt="" loading="lazy"></figure>
<p>假设 Module X 同时依赖 Library A 和 Library B，而 Library A 通过 <code>implementation</code> 引用 Library C，而 Library B 则使用 <code>api</code> 引用 Library D。</p>
<p>这时候，Module X 是不能够直接使用 Library C 的，但却能够直接使用 Library D。因为 <code>implementation</code> 可以让 Module 在编译时“隐藏”自己使用的依赖，而 <code>api</code> 则会“传递”自己的依赖。</p>
<p>同时，如果 <code>api</code> 依赖项更改了其外部 API，Gradle 会在编译时重新编译所有有权访问该依赖项的 Module，因此，拥有大量的 <code>api</code> 依赖项会显著增加构建时间。</p>
<p>除非要将依赖项的 API 公开给单独的 Module，否则应使用 <code>implementation</code> 依赖项。</p>
<h1 id="compileonly">compileOnly</h1>
<p>Gradle 只会将依赖项添加到编译类路径，也就是说，不会将其添加到构建输出。</p>
<p>如果您创建 Module 时在编译期间需要相应依赖项，但它在运行时可有可无，此配置会很有用。</p>
<p>如果您使用 <code>compileOnly</code>，那么您的 Module 必须包含一个运行时条件，用于检查是否提供了相应依赖项，然后适当地改变该 Module 的行为，以使该 Module 在未提供相应依赖项的情况下仍可正常运行。</p>
<p>这样做不会添加不重要的暂时依赖项，因而有助于减小最终 APK 的大小。此配置的行为类似于 <code>provided</code>（现已弃用）。</p>
<h1 id="runtimeonly">runtimeOnly</h1>
<p>Gradle 只会将依赖项添加到构建输出，以便在运行时使用。也就是说，不会将其添加到编译类路径。</p>
<p>此配置的行为类似于 <code>apk</code>（现已弃用）。</p>
<h1 id="annotationprocessor">annotationProcessor</h1>
<p>我们在使用一些包含注解的第三方库时经常可以见到它，比如 <code>Dagger</code> 等。</p>
<p>如需添加对作为注解处理器的库的依赖，可以使用 <code>annotationProcessor</code> 配置将其添加到注解处理器的类路径。</p>
<p>使用此配置可以将编译类路径与注释处理器类路径分开，从而提高构建性能。</p>
<h1 id="lintchecks">lintChecks</h1>
<p>使用此配置可以添加您希望 Gradle 在构建项目时执行的 lint 检查。</p>
<p>需要注意，使用 Android Gradle Plugin 3.4.0 及更高版本时，此依赖项配置不再将 lint 检查打包在 AAR 项目中。</p>
<h1 id="lintpublish">lintPublish</h1>
<p>在 AAR 项目中使用此配置可以添加您希望 Gradle 编译成 <code>lint.jar</code> 文件并打包在 AAR 中的 lint 检查。这会使得使用 AAR 的项目也应用这些 lint 检查。</p>
<h1 id="testimplementation">testImplementation</h1>
<p>只在单元测试代码的编译以及最终打包测试 APK 时有效。</p>
<p>默认新建项目中 <code>JUnit</code> 就是以此配置依赖的，同时可以看到默认项目目录中除了我们一般写业务代码的 <code>/src/main</code> 目录外，还有 <code>/src/test</code> 目录供我们写本地单元测试，<code>testImplementation</code> 就可以为其 Source Set 添加依赖。</p>
<h1 id="androidtestimplementation">androidTestImplementation</h1>
<p>只在单元测试代码的编译以及最终打包测试 APK 时有效。</p>
<p>默认新建项目中 <code>Espresso</code> 就是以此配置依赖的，同时可以看到默认项目目录中除了我们一般写业务代码的 <code>/src/main</code> 目录外，还有 <code>/src/androidTest</code> 目录供我们写仪器化单元测试、UI 测试等，<code>androidTestImplementation</code> 就可以为其 Source Set 添加依赖。</p>
<h1 id="releaseimplementation-debugimplementation">releaseImplementation / debugImplementation</h1>
<p>仅仅针对 Release 或 Debug 模式的编译和最终的 Release / Debug Apk 打包。</p>
<p>DiDi 团队著名框架 <code>DoKit</code> 就是以此配置依赖的，可以针对 Release 和 Debug 采用不同的处理。</p>
<h1 id="compile">compile</h1>
<p>该配置已弃用。</p>
<p>与 <code>api</code> 类似，Gradle 会将依赖项添加到编译类路径和构建输出，并将依赖项导出到其他 Module。</p>
<h1 id="apk">apk</h1>
<p>该配置已弃用。</p>
<p>与 <code>runtimeOnly</code> 类似，Gradle 只会将依赖项添加到构建输出，以便在运行时使用，不会将其添加到编译类路径。</p>
<h1 id="provided">provided</h1>
<p>该配置已弃用。</p>
<p>与 <code>compileOnly</code> 类似，Gradle 只会将依赖项添加到编译类路径，不会将其添加到构建输出。</p>
<hr>
<h1 id="参考">参考</h1>
<ul>
<li><a href="https://developer.android.google.cn/studio/build/dependencies">添加构建依赖项 | Android 开发者 | Android Developers</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[『Android Studio』用 Gradle 实现自动签名]]></title>
        <id>https://LiarrDev.github.io/post/Auto-Signing-with-Gradle-in-Android-Studio/</id>
        <link href="https://LiarrDev.github.io/post/Auto-Signing-with-Gradle-in-Android-Studio/">
        </link>
        <updated>2021-04-29T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>当我们需要接入一些三方授权功能时，提供授权的平台往往需要校验我们 App 的合法性，而应用签名，就是校验应用是否合法的手段，国内像是 QQ 和微信提供的第三方登录能力，就是通过校验签名和包名来提供授权的。</p>
<p>在开发调试时，我们总是习惯于修改完代码后直接 Run 项目到设备上，因为打出一个 Release 包再签名这个步骤一旦重复操作，会降低我们的效率，但调试，又免不了频繁的运行。</p>
<p>既然无法避免，那我们能不能像平常 Debug 一样，一键把签名和 Run 都执行了呢？</p>
<p>首先要明确，虽然平常 Debug 我们只需要点击工具栏的「Run」按钮，项目就会编译并运行到我们的设备上，但实际上它也是进行了签名操作的。</p>
<p>『Android Studio』默认为我们生成了一个用于调试的签名，目录为 <code>~/Users/&lt;User&gt;/.android/debug.keystore</code>，这样即使我们不为项目配置任何签名，项目仍能够使用这个调试签名运行到设备中。</p>
<p>根据我们对应用签名的经验，除了签名文件，我们还需要另外三个参数，分别是 Key store password、Key alias 和 Key password。</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1619369009040.png" alt="" loading="lazy"></figure>
<p>当然，这个用于调试的签名文件也有。其中 Key store password 和 Key password 的值都为 <code>android</code>，Key alias 的值为 <code>androiddebugkey</code>。</p>
<p>下面，我就使用这个用于调试的签名，来实现自动签名的操作。</p>
<p>首先打开「Project Structure」，选择「Modules」中选择对应的 Module 配置签名，新建一个签名配置：</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1619369019894.png" alt="" loading="lazy"></figure>
<p>然后填写对应的参数：</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1619369045068.png" alt="" loading="lazy"></figure>
<p>确认后，『Android Studio』就会在对应 Module 的「build.gradle」文件中为我们生成签名信息：</p>
<pre><code class="language-Groovy">android {
    ...
    signingConfigs {
        release {
            storeFile file('/Users/liarr/.android/debug.keystore')
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }
}
</code></pre>
<p>这里我使用的是调试的签名，实际项目中一般不会配置此签名，所以相应的参数也不会一致。</p>
<p>最后我们还需配置：</p>
<pre><code class="language-Groovy">android {
    ...
    buildTypes {
        release {
            ...
            signingConfig signingConfigs.release
        }
        debug {
            ...
            signingConfig signingConfigs.release
        }
    }
}
</code></pre>
<p>这样，当我们运行项目时，它就会通过 Gradle 配置对我们的应用签名了。</p>
<p>实际上，你完全可以手动在 Gradle 文件中输入以上代码进行配置而无需使用「Project Structure」。</p>
<p>配置完成后，你甚至可以直接使用 Gradle Task 来快速生成 Debug 和 Release 的安装包。</p>
<p>另外，假如我们的项目存储在代码托管平台或选择开源的话，将签名信息直接明文写在 Gradle 文件中并不是一个好主意，应当创建一个单独的属性文件来存储安全信息并在编译文件中引用该文件。</p>
<p>比如在项目的根目录下创建一个名为「keystore.properties」的文件。此文件应当包含您的签名信息：</p>
<pre><code class="language-Properties">storePassword=android
keyPassword=android
keyAlias=androiddebugkey
storeFile=/Users/liarr/.android/debug.keystore
</code></pre>
<p>然后在 Gradle 文件中配置：</p>
<pre><code class="language-Groovy">def keystorePropertiesFile = rootProject.file(&quot;keystore.properties&quot;)
def keystoreProperties = new Properties()
keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
android {
    ...
    signingConfigs {
        config {
            keyAlias keystoreProperties['keyAlias']
            keyPassword keystoreProperties['keyPassword']
            storeFile file(keystoreProperties['storeFile'])
            storePassword keystoreProperties['storePassword']
        }
    }
    buildTypes {
        release {
            ...
            signingConfig signingConfigs.config
        }
        debug {
            ...
            signingConfig signingConfigs.config
        }
    }
}
</code></pre>
<p>不要忘了将签名信息加入忽略清单：</p>
<pre><code class="language-GitIgnore"># Ignore Signing Config
/keystore.properties
</code></pre>
<p>这样我们的签名信息就更安全了。</p>
<hr>
<h1 id="参考内容">参考内容</h1>
<ul>
<li><a href="https://developer.android.google.cn/studio/publish/app-signing">为应用签名 | Android 开发者 | Android Developers</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[一文带你简单了解 Hosts]]></title>
        <id>https://LiarrDev.github.io/post/A-Brief-Overview-of-Hosts/</id>
        <link href="https://LiarrDev.github.io/post/A-Brief-Overview-of-Hosts/">
        </link>
        <updated>2021-04-22T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>在日常上网中我经常需要跟 Hosts 打交道，比如访问部分海外网站，或者是在外面访问公司内网，又或者日常开发测试，都需要修改 Hosts。</p>
<p>不要觉得 Hosts 很高深，说不定你在平时的网上冲浪中也会接触到。</p>
<p>今天就来简单科普一下 Hosts，让小白也能够轻松了解它。</p>
<h1 id="hosts-是什么">Hosts 是什么</h1>
<p>Hosts 是一个没有扩展名的系统文件，其作用就是将一些常用的网址域名与其对应的 IP 地址建立一个关联数据库，当用户在浏览器中输入一个需要登录的网址时，系统会首先自动从 Hosts 文件中寻找对应的 IP 地址，一旦找到，系统会立即打开对应网页，如果没有找到，则系统会再将网址提交 DNS 域名解析服务器进行 IP 地址的解析。</p>
<p>如果这段话讲得很懵逼，那我举个例子，比如说百度的 IP 是 <code>14.215.177.38</code>，这一串没有任何意义和规律的东西很难记对吧，所以百度就搞了一个域名叫 <code>www.baidu.com</code>，那么你在浏览器中只需要输入这个域名就能够访问到百度的首页。</p>
<p>但是实际上浏览器是通过 IP 地址才能找到该网站所在的服务器并进行访问的。在 Hosts 文件中没有添加这个映射的情况下，系统会先将这个域名告诉 DNS 域名解析服务器，然后等解析出 IP 后再进行访问；而如果在 Hosts 中添加了这个映射，那么就可以跳过解析 IP 这一步，因为它可以直接从 Hosts 中获取到 IP，因此访问速度就会变得快很多。</p>
<p>访问一些受限制的网站也是同理，DNS 域名解析服务器解析不到对应的 IP，所以你也就无法正常访问了。</p>
<p>可以说，操作系统的 Hosts 文件则是有着类似本地 DNS 服务器的作用，而且比网络上的 DNS 具有更高的优先级。</p>
<p>如果这样解释还是不明白，那我再打个比方：</p>
<p>有一个地点，域名就相当于这个地址，比如广州塔，挺好记的对吧，而 IP 就相当于经纬度，比如我告诉你东经 113.32°，北纬 23.11°，相信你是崩溃的，而 Hosts 的作用就是你有个小本本记录地址和经纬度的映射关系，那么只要我告诉你地址，你不需要再去用专业的工具查，而是马上打开你的小本本，然后就可以根据经纬度立刻在地图上找到定位，这就是效率。</p>
<p>而限制访问就相当于，存在一个军事基地，但是由于军事基地是保密的，所以你在地图上不一定能找到它，这时候 Hosts 的作用就是，有一个内部人员告诉你这个军事基地的经纬度，那你就记在了小本本上，下次你想找这个军事基地的时候，马上打开你的小本本，就有答案了。</p>
<h1 id="hosts-的作用">Hosts 的作用</h1>
<p>根据上述这样的特点，Hosts 文件可以应用在很多场景，包括但不限于以下几种：</p>
<h2 id="加快域名解析">加快域名解析</h2>
<p>上面也提到，对于要经常访问的网站，我们可以通过在 Hosts 中配置域名和 IP 的映射关系，提高域名解析速度。由于有了映射关系，当我们输入域名计算机就能很快解析出 IP，而不用请求网络上的 DNS 服务器，同时也能绕过 DNS 污染与劫持。</p>
<h2 id="方便局域网用户">方便局域网用户</h2>
<p>在很多单位的局域网中，会有服务器提供给用户使用。但由于局域网中一般很少架设 DNS 服务器，访问这些服务器时，要输入难记的 IP 地址，这对不少人来说相当麻烦。</p>
<p>可以分别给这些服务器取个容易记住的名字，然后在 Hosts 中建立 IP 映射，这样以后访问的时候，只要输入这个服务器的名字就行了。</p>
<h2 id="屏蔽网站">屏蔽网站</h2>
<p>有很多网站不经过用户同意就将各种各样的插件安装到你的计算机中，其中有些说不定就是木马或病毒。对于这些网站我们可以利用 Hosts 把该网站的域名映射到错误的 IP 或本地计算机的 IP，这样就达到屏蔽访问的效果，同样也可以用于屏蔽不健康网站、屏蔽垃圾广告网址等。</p>
<h2 id="虚拟域名">虚拟域名</h2>
<p>很多时候，网站建设者需要把软环境搭建好，再进行上传调试。但类似于邮件服务，则需要使用域名来辅助调试，这时就可以将本地 IP 地址与一个虚拟域名做地址指向，就可以达到要求的效果，且无需花费。</p>
<h2 id="域名重定向">域名重定向</h2>
<p>开发或测试应用时，利用 Hosts 将域名临时指向到测试服务器 IP，可以方便自己测试，同时又不影响他人和线上的应用。</p>
<h1 id="总结">总结</h1>
<p>说了那么多，其实 Hosts 并不神秘，简单理解它就是个电脑本机的「IP 地址 ↔ 域名」的对应表，系统或任何软件都会优先先检查 Hosts 里面有没对应的配置，之后才会去查询网上的 DNS 服务器。</p>
<p>因此你可以利用 Hosts 做很多有趣、有用的事情。</p>
<p>当然，本文只是一个简单的科普，追根溯源其实还有很多东西并没有详细聊到，比如 DNS 等，有兴趣的朋友可以自行深挖。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android 对 TextView 和 EditText 的 Drawable 监听点击]]></title>
        <id>https://LiarrDev.github.io/post/Listening-Clicks-on-Drawables-of-TextView-or-EditText/</id>
        <link href="https://LiarrDev.github.io/post/Listening-Clicks-on-Drawables-of-TextView-or-EditText/">
        </link>
        <updated>2021-04-12T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>之前在『<a href="https://liarrdev.github.io/post/Add-Icons-for-TextView-or-EditText-in-Android/">Android 为 TextView 和 EditText 添加 ICON</a>』一文中介绍了如何为 <code>TextView</code> 和 <code>EditText</code> 添加 <code>Drawable</code> 作为指示，使交互更加友好，开发更方便，但仍遗留了一个问题，我们能否监听该 <code>Drawable</code> 的点击事件？</p>
<p>这是一个很常见的需求，比如在密码输入框中添加一个用于切换是否显示明文的按钮：</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1563355131994.gif" alt="" loading="lazy"></figure>
<p>你当然可以使用 <code>EditText</code> 加 <code>ImageButton</code> 组合的方式，这不在本文的讨论范围内。</p>
<p>那么 <code>TextView</code> 或者是继承自 <code>TextView</code> 的 <code>EditText</code> 有没有提供监听 <code>Drawable</code> 点击事件的方法呢？</p>
<p>很遗憾，并没有。</p>
<p>因此，我们需要自己去实现对应的点击逻辑。</p>
<p>如果你上网搜索对应的内容，介绍的文章真不少，你复制下来到自己的项目上运行，效果或许也的确能够达到你的预期，但我这里要泼个冷水，你拷贝的这份代码很可能是错的——这是我踩过的坑。</p>
<p>其实原理大家都懂，就是实现上可能会忽略一些细节。</p>
<p>我简单先说下原理：</p>
<p>假如我们能够测量出 <code>Drawable</code> 在控件内的区域，就可以对该区域内的触摸事件作监听，也就能够实现监听点击事件的效果。</p>
<p>而难点，恰好就在于如何测量出 <code>Drawable</code> 所在区域，这也是网上很多文章犯错的地方。</p>
<p>由几何知识可以知道，在二维平面中，只要确定了 2 个顶点的坐标，以这两个顶点分别向两坐标轴作垂线就可以绘制出一个矩形区域。需要明确的是，在 Android 中坐标点的计算和我们在数学上不太相同，Android 中坐标原点为左上角顶点，往右为 X 轴正方向，往下为 Y 轴正方向。</p>
<p>接下来只需画个图就能清楚明了。</p>
<p>以 <code>DrawableLeft</code> 为例：</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1618149375304.png" alt="" loading="lazy"></figure>
<p><code>DrawableLeft</code> 在 X 轴方向上的距离很容易计算，其左侧与 Y 轴的距离实际上就是该 <code>View</code> 的 <code>PaddingLeft</code>，其右侧与 Y 轴的距离就是 <code>PaddingLeft</code> 和该 <code>Drawable</code> 的宽度之和。</p>
<p>但 <code>DrawableLeft</code> 在 Y 轴方向上的距离要稍微复杂一些，因为它会受到 <code>DrawableTop</code> 以及文字区域高度的影响，所以我们不能够直接获取。</p>
<p>这里解释一下，<code>DrawableTop</code> 的影响应该很好理解，也很好处理；文字区域高度的影响，是指不包含 <code>Drawable</code> 的实际文字区域，当文字区域的高度高于 <code>DrawableLeft</code> 的高度时，我们如果不进行计算，获取到的高度可能会比实际上要高，这会造成我们测量出来的区域比实际区域要大。</p>
<p>计算的方法也不难，我们只需要获取到 <code>DrawableLeft</code> 中心在 Y 轴方向上的位置，再增减 <code>Drawable</code> 本身一半的高度，即可得到其上下边界到 X 轴的距离。</p>
<p>这样我们就把 <code>DrawableLeft</code> 所在区域计算出来了，同理 <code>DrawableTop</code> 也能够使用相同的方法计算。</p>
<p>接下来就是 <code>DrawableRight</code>：</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1618149402155.png" alt="" loading="lazy"></figure>
<p><code>DrawableRight</code> 右侧在 X 轴方向上的距离实际上就是该 <code>View</code> 的总宽度与 <code>PaddingRight</code> 之差，其左侧在 X 轴方向上的距离就是其右侧在 X 轴方向的距离与该 <code>Drawable</code> 的宽度之差。</p>
<p><code>DrawableRight</code> 在 Y 轴方向上的距离与 <code>DrawableLeft</code> 的计算方式无异，不再赘述。</p>
<p>用同样的方法，我们也能够计算出 <code>DrawableBottom</code> 的区域。</p>
<p>原理介绍完了，就该上代码了，为了更好的封装，我们可以自定义 <code>View</code> 来实现，以 <code>TextView</code> 为例：</p>
<pre><code class="language-Java">public class DrawableTextView extends AppCompatTextView {

    final int DRAWABLE_LEFT = 0;
    final int DRAWABLE_TOP = 1;
    final int DRAWABLE_RIGHT = 2;
    final int DRAWABLE_BOTTOM = 3;

    Drawable mDrawableLeft, mDrawableTop, mDrawableRight, mDrawableBottom;

    public DrawableTextView(@NonNull Context context) {
        super(context);
    }

    public DrawableTextView(@NonNull Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public DrawableTextView(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) { 
        super(context, attrs, defStyleAttr);
    }

    @Override
    public boolean onTouchEvent(MotionEvent event) {
        if (event.getAction() == MotionEvent.ACTION_DOWN) {
            if (onDrawableClickListener != null) {
                mDrawableLeft = getCompoundDrawables()[DRAWABLE_LEFT];
                mDrawableTop = getCompoundDrawables()[DRAWABLE_TOP];
                mDrawableRight = getCompoundDrawables()[DRAWABLE_RIGHT];
                mDrawableBottom = getCompoundDrawables()[DRAWABLE_BOTTOM];
                if (touchDrawableLeft(event)) {
                    onDrawableClickListener.onDrawableLeftClick();
                }
                if (touchDrawableTop(event)) {
                    onDrawableClickListener.onDrawableTopClick();
                }
                if (touchDrawableRight(event)) {
                    onDrawableClickListener.onDrawableRightClick();
                }
                if (touchDrawableBottom(event)) {
                    onDrawableClickListener.onDrawableBottomClick();
                }
            }
        }
        return super.onTouchEvent(event);
    }

    private boolean touchDrawableLeft(MotionEvent event) {
        if (mDrawableLeft == null) {
            return false;
        }
        int drawableHeight = mDrawableLeft.getIntrinsicHeight();
        int drawableWidth = mDrawableLeft.getIntrinsicWidth();
        int topOfDrawable = getCompoundPaddingTop();
        int bottomOfDrawable = getHeight() - getCompoundPaddingBottom();
        double drawableCenterY = 0.5 * (bottomOfDrawable - topOfDrawable) + topOfDrawable;
        Rect bounds = new Rect(getPaddingLeft(),
                (int) (drawableCenterY - 0.5 * drawableHeight),
                getPaddingLeft() + drawableWidth,
                (int) (drawableCenterY + 0.5 * drawableHeight)
        );
        return bounds.contains((int) event.getX(), (int) event.getY());
    }

    private boolean touchDrawableTop(MotionEvent event) {
        if (mDrawableTop == null) {
            return false;
        }
        int drawableHeight = mDrawableTop.getIntrinsicHeight();
        int drawableWidth = mDrawableTop.getIntrinsicWidth();
        int leftOfDrawable = getCompoundPaddingLeft();
        int rightOfDrawable = getWidth() - getCompoundPaddingRight();
        double drawableCenterX = 0.5 * (rightOfDrawable - leftOfDrawable) + leftOfDrawable;
        Rect bounds = new Rect((int) (drawableCenterX - 0.5 * drawableWidth),
                getPaddingTop(),
                (int) (drawableCenterX + 0.5 * drawableWidth),
                getPaddingTop() + drawableHeight);
        return bounds.contains((int) event.getX(), (int) event.getY());
    }

    private boolean touchDrawableRight(MotionEvent event) {
        if (mDrawableRight == null) {
            return false;
        }
        int drawableHeight = mDrawableRight.getIntrinsicHeight();
        int drawableWidth = mDrawableRight.getIntrinsicWidth();
        int topOfDrawable = getCompoundPaddingTop();
        int bottomOfDrawable = getHeight() - getCompoundPaddingBottom();
        double drawableCenterY = 0.5 * (bottomOfDrawable - topOfDrawable) + topOfDrawable;
        Rect bounds = new Rect(getWidth() - getPaddingRight() - drawableWidth,
                (int) (drawableCenterY - 0.5 * drawableHeight),
                getWidth() - getPaddingRight(),
                (int) (drawableCenterY + 0.5 * drawableHeight));
        return bounds.contains((int) event.getX(), (int) event.getY());
    }

    private boolean touchDrawableBottom(MotionEvent event) {
        if (mDrawableBottom == null) {
            return false;
        }
        int drawHeight = mDrawableBottom.getIntrinsicHeight();
        int drawWidth = mDrawableBottom.getIntrinsicWidth();
        int leftOfDrawable = getCompoundPaddingLeft();
        int rightOfDrawable = getWidth() - getCompoundPaddingRight();
        double drawableCenterX = 0.5 * (rightOfDrawable - leftOfDrawable) + leftOfDrawable;
        Rect bounds = new Rect((int) (drawableCenterX - 0.5 * drawWidth),
                getHeight() - getPaddingBottom() - drawHeight,
                (int) (drawableCenterX + 0.5 * drawWidth),
                getHeight() - getPaddingBottom());
        return bounds.contains((int) event.getX(), (int) event.getY());
    }

    public interface OnDrawableClickListener {
        void onDrawableLeftClick();
        void onDrawableTopClick();
        void onDrawableRightClick();
        void onDrawableBottomClick();
    }

    private OnDrawableClickListener onDrawableClickListener;

    public void setOnDrawableClickListener(OnDrawableClickListener listener) {
        this.onDrawableClickListener = listener;
    }
}
</code></pre>
<p>代码不短，但我们理解了上面的原理之后也很容易看懂。</p>
<p>我在这里定义了一个 <code>OnDrawableClickListener</code> 接口，用于分别回调 <code>TextView</code> 四个方向上的点击事件。</p>
<p>重点内容在重写的 <code>onTouchEvent()</code> 方法内，这个方法用于监听 <code>TextView</code> 的触摸事件。在该方法内，首先通过 <code>getCompoundDrawables()</code> 方法获取到 <code>Drawable</code> 数组，并通过下标分别拿到四个方向上的 <code>Drawable</code> 对象。紧接着判断触摸事件是否落在对应的 <code>Drawable</code> 区域内，然后回调给 <code>OnDrawableClickListener</code>。</p>
<p>触摸区域的判断就根据上面原理写就可以了，解释下几个方法。</p>
<p><code>getIntrinsicWidth()</code> 和 <code>getIntrinsicHeight()</code> 分别用于获取当前 <code>Drawable</code> 对象的宽高。</p>
<p><code>getCompoundPaddingLeft()</code> 获取的则是 <code>TextView</code> 左边界到文字实际显示区域的距离，我们可以看下源码：</p>
<pre><code class="language-Java">/**
 * Returns the left padding of the view, plus space for the left Drawable if any.
 */
public int getCompoundPaddingLeft() {
    final Drawables dr = mDrawables;
    if (dr == null || dr.mShowing[Drawables.LEFT] == null) {
        return mPaddingLeft;
    } else {
        return mPaddingLeft + dr.mDrawablePadding + dr.mDrawableSizeLeft;
    }
}
</code></pre>
<p>也就是说，当 <code>DrawableLeft</code> 不存在时，它取得的值是 <code>PaddingLeft</code>，而当 <code>DrawableLeft</code> 存在时，它取得的值是 <code>PaddingLeft</code>、<code>DrawableLeft</code> 的宽度、<code>DrawablePadding</code> 三者之和。其他方向也同理。</p>
<p>回到刚刚还没解释的问题，当我们知道了文字显示区域距离坐标轴的距离时，<code>Drawable</code> 的中心点也就可以计算出来了。</p>
<figure data-type="image" tabindex="4"><img src="https://LiarrDev.github.io/post-images/1618149477448.png" alt="" loading="lazy"></figure>
<p>你可能会有疑问，『<a href="https://liarrdev.github.io/post/Add-Icons-for-TextView-or-EditText-in-Android/">Android 为 TextView 和 EditText 添加 ICON</a>』一文不是提到可以通过设置 <code>Gravity</code> 来指定内部元素的对齐方式，那会不会影响到 <code>Drawable</code> 位置的计算？</p>
<p>并不会，因为 <code>Gravity</code> 实际上指定的是文字在文字区域内的显示位置，而不是 <code>Drawable</code> 的位置，即 <code>Drawable</code> 的位置相对于文字区域来说，依然是居中的，所以我们通过文字区域来计算 <code>Drawable</code> 的中心点仍旧可行。</p>
<p>最后，我们将 <code>Drawable</code> 所在的矩形区域计算出来，并判断触摸事件在该 <code>View</code> 上的落点是否在 <code>Drawable</code> 的矩形区域内，就能够判断该 <code>Drawable</code> 是否被点击。</p>
<p>这个自定义的控件写完后，只需替换原来的 <code>TextView</code> 即可，属性与原来无异。</p>
<p>对于继承自 <code>TextView</code> 的 <code>EditText</code> 也同理，使用上面的自定义方法同样适用。</p>
<p>我这里就使用自定义的 <code>EditText</code> 来写一下文章开头的密码显隐功能。</p>
<p>首先替换布局文件中原来的 <code>EditText</code> 控件：</p>
<pre><code class="language-XML">&lt;com.example.DrawableEditText
    android:id=&quot;@+id/password&quot;
    android:layout_width=&quot;match_parent&quot;
    android:layout_height=&quot;wrap_content&quot;
    android:drawableRight=&quot;@drawable/ic_eye_off&quot;
    android:inputType=&quot;textPassword&quot; /&gt;
</code></pre>
<p>在逻辑代码中响应其点击事件：</p>
<pre><code class="language-Java">private boolean passwordVisible = false;

void switchPasswordVisibility(final DrawableEditText editText) {
    editText.setOnDrawableClickListener(new DrawableEditText.OnDrawableClickListener() {
        @Override
        public void onDrawableLeftClick() {}

        @Override
        public void onDrawableTopClick() {}

        @Override
        public void onDrawableRightClick() {
            Drawable drawable;
            if (passwordVisible) {
                drawable = getResources().getDrawable(R.drawable.ic_eye_off);
//              editText.setInputType(InputType.TYPE_TEXT_VARIATION_PASSWORD | InputType.TYPE_CLASS_TEXT);
                editText.setTransformationMethod(PasswordTransformationMethod.getInstance());
            } else {
                drawable = getResources().getDrawable(R.drawable.ic_eye_on);
//              editText.setInputType(InputType.TYPE_TEXT_VARIATION_VISIBLE_PASSWORD);
                editText.setTransformationMethod(HideReturnsTransformationMethod.getInstance());
            }
            passwordVisible = !passwordVisible;
            drawable.setBounds(0, 0, drawable.getMinimumWidth(), drawable.getMinimumHeight());
            editText.setCompoundDrawables(null, null, drawable, null);
        }

        @Override
        public void onDrawableBottomClick() {}
    });
}
</code></pre>
<p>因为只设置了 <code>DrawableRight</code>，所以这里只需要写入 <code>DrawableRight</code> 的事件即可。</p>
<p>效果如下：</p>
<figure data-type="image" tabindex="5"><img src="https://LiarrDev.github.io/post-images/1563361469785.gif" alt="" loading="lazy"></figure>
<p>虽然我们在 <code>EditText</code> 的控件属性中常用 <code>android:inputType</code> 属性来设置其为密码的样式，但在这里并不建议使用 <code>setInputType()</code> 方法来作更改，而是使用 <code>setTransformationMethod()</code> 方法，主要是因为 <code>setInputType()</code> 方法有两个坑。</p>
<p>其一，密码从可见状态切回密文状态时，我们以在控件属性中使用的经验，一般会将其设置为：</p>
<pre><code class="language-Java">editText.setInputType(InputType.TYPE_TEXT_VARIATION_PASSWORD);
</code></pre>
<p>但是运行之后你会发现，并不会生效：</p>
<figure data-type="image" tabindex="6"><img src="https://LiarrDev.github.io/post-images/1563362680037.gif" alt="" loading="lazy"></figure>
<p>可以看我上方注释掉的代码中需要同时设置两个属性才能生效：</p>
<pre><code class="language-Java">editText.setInputType(InputType.TYPE_TEXT_VARIATION_PASSWORD | InputType.TYPE_CLASS_TEXT);
</code></pre>
<p>对应控件属性中的：</p>
<pre><code class="language-XML">&lt;EditText
    ...
    android:inputType=&quot;textPassword&quot; /&gt;
</code></pre>
<p>因为 <code>textPassword</code> 对应的 <code>InputType</code> 的值为 <code>129</code>，而 <code>InputType.TYPE_TEXT_VARIATION_PASSWORD</code> 对应的值是 <code>128</code>，所以很明显需要和值为 <code>1</code> 的 <code>InputType.TYPE_CLASS_TEXT</code> 做按位或运算。</p>
<p>其二，使用 <code>setInputType()</code> 方法转换后会出现样式不一的情况：</p>
<figure data-type="image" tabindex="7"><img src="https://LiarrDev.github.io/post-images/1563362022713.gif" alt="" loading="lazy"></figure>
<p>可以见到，当切换为密文状态时，点和点之间的距离增大了，与默认样式不统一，会造成用户困扰。</p>
<p>而使用 <code>setTransformationMethod()</code> 就可以避免上面的情况。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Android 为 TextView 或 EditText 添加 ICON]]></title>
        <id>https://LiarrDev.github.io/post/Add-Icons-for-TextView-or-EditText-in-Android/</id>
        <link href="https://LiarrDev.github.io/post/Add-Icons-for-TextView-or-EditText-in-Android/">
        </link>
        <updated>2021-03-15T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>在应用开发中，为了友好的用户体验，我们常常需要在文字旁边添加 ICON，因为用户对图形的感知往往要强于文字，比如这样子：</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1562338869852.png" alt="分类导航" loading="lazy"></figure>
<p>大多数新手看一眼觉得很简单，使用 <code>ImageView</code> 和 <code>TextView</code> 组合即可。</p>
<p>的确，这是一种很简单的方法，但如上图中使用 <code>GridLayout</code> 来展示多个子项，明明 ICON 和文字属于同一项，组合写法会导致代码成倍增加，再加上需要用一个父容器（如 <code>LinearLayout</code>）来包裹这两个子项，布局文件就如老太太的裹脚布——又长又臭。</p>
<p>当然，你也可以把这个组合抽取出来，做成 <code>Adapter</code> 通用，尽管布局清晰了，但逻辑代码也增加不少。</p>
<p>实际上，Android 本身就为 <code>TextView</code> 提供了此功能，我们只需一行代码就可以使用：</p>
<pre><code class="language-XML">&lt;TextView
    ...
    android:drawableTop=&quot;@drawable/ic_drawable&quot; /&gt; 
</code></pre>
<p>该属性可以为 <code>TextView</code> 在某一方向上添加对应的 <code>Drawable</code>，其他方向只需修改属性中的方向名称即可。</p>
<p><code>AppCompat</code> 提供了一些 <code>Compat</code> 属性，所以你也可以这样写：</p>
<pre><code class="language-XML">&lt;TextView
    ...
    app:drawableTopCompat=&quot;@drawable/ic_drawable&quot; /&gt; 
</code></pre>
<p>如果你想调整 <code>Drawable</code> 和文字的间隔，可以使用这个属性：</p>
<pre><code class="language-XML">&lt;TextView
    ...
    android:drawablePadding=&quot;16dp&quot;
    app:drawableTopCompat=&quot;@drawable/ic_drawable&quot; /&gt; 
</code></pre>
<p>当 <code>Drawable</code> 尺寸过大时，你可能会注意到 <code>Drawable</code> 和文字并不是居中的，这时你要牢记，这个 <code>Drawable</code> 也是 <code>TextView</code> 的一部分：</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1562338892794.png" alt="TextView 范围" loading="lazy"></figure>
<p>对于 <code>View</code> 或 <code>ViewGroup</code> 内部元素的对齐方式，我们通常可以使用：</p>
<pre><code class="language-XML">&lt;TextView
    ...
    android:gravity=&quot;center&quot;
    app:drawableTopCompat=&quot;@drawable/ic_drawable&quot; /&gt; 
</code></pre>
<p>假如你需要在逻辑代码中动态修改该 <code>Drawable</code>，同样提供支持：</p>
<pre><code class="language-Java">textView.setCompoundDrawablesWithIntrinsicBounds(getResources().getDrawable(R.drawable.ic_drawable), null, null, null); 
</code></pre>
<p><code>setCompoundDrawablesWithIntrinsicBounds()</code> 方法接收四个参数，分别是四个方向上的 <code>Drawable</code> 资源，在对应方向传入所需的 <code>Drawable</code> 即可。</p>
<p>该方法同时也支持以资源 ID 的方式传入 <code>Drawable</code>，所以也可以这样写：</p>
<pre><code class="language-Java">textView.setCompoundDrawablesWithIntrinsicBounds(R.drawable.ic_drawable, 0, 0, 0); 
</code></pre>
<p>当你觉得这个功能挺好用时，假如美工让你调整一下 <code>Drawable</code> 的大小，你可能一下子就懵了，似乎找不到这个属性，还不如 <code>ImageView</code> 和 <code>TextView</code> 组合来得方便呀。</p>
<p>的确，在布局文件中没有调整 <code>Drawable</code> 尺寸的属性，但你要记得，这是一个 <code>Drawable</code>，既然是 <code>Drawable</code>，我们就可以通过逻辑代码控制它的尺寸：</p>
<pre><code class="language-Java">Drawable drawable = getResources().getDrawable(R.drawable.ic_account);
drawable.setBounds(0, 0, 180, 180);                     // 设置图片的大小
textView.setCompoundDrawables(drawable, null, null, null);   // 设置图片的位置 
</code></pre>
<p><code>Drawable</code> 的 <code>setBounds()</code> 方法接收四个参数，四个坐标形成一个矩形，<code>Drawable</code> 将在被绘制在这个矩形区域内。</p>
<p>接着调用的 <code>setCompoundDrawables()</code> 和上面提到的 <code>setCompoundDrawablesWithIntrinsicBounds()</code> 类似，将 <code>Drawable</code> 传入到对应位置即可。</p>
<p>另外我们知道，<code>EditText</code> 实际上是继承于 <code>TextView</code> 的，那么 <code>EditText</code> 也会有相同的属性，所以我们可以轻而易举的实现类似的功能：</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1562421497192.png" alt="EditText With ICON" loading="lazy"></figure>
<p>属性和方法基本和 <code>TextView</code> 一致，不再重复贴代码。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[用『Hexo』搭建个人博客]]></title>
        <id>https://LiarrDev.github.io/post/Build-a-Blog-with-Hexo/</id>
        <link href="https://LiarrDev.github.io/post/Build-a-Blog-with-Hexo/">
        </link>
        <updated>2021-02-07T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>其实很多人都想过有一个属于自己的网站，无奈因为技术门槛，大多数人都没能实现这个小小的心愿。</p>
<p>说实话，对于技术人或者是互联网从业者来说，一个属于自己的个人网站是很常见的，由于我们通常会将其用于记录我们的一些笔记、知识、生活等等，因此也会将其称为博客。</p>
<p>“博客”这个词，说起来也有点儿久远了，甚至现在很多人都已经不知道这个词，它其实是一个音译词，英文叫“Blog”，而这个词也是一个混成词，源自“Web Log”，即网络日志，更早期的音译其实叫“部落格”，是一种通常由个人管理、不定期张贴新文章的网站。</p>
<p>国内也有很多博客写作平台，比如偏技术向的『CSDN』和『掘金』，偏大众向的『简书』等等，注册个账号就能开始写作，那为什么还要自己动手去搭建一个博客呢？</p>
<p>从我个人看来，有三个原因：</p>
<ul>
<li><strong>广告</strong>。商业平台需要盈利，广告是重要的来源，平台往往会向作者的博客插入广告而不需要经过作者的同意。</li>
<li><strong>样式</strong>。商业平台通常对于所有的博客，都只会提供单一的页面展示样式，没有个性化定制。</li>
<li><strong>监管</strong>。文章发表在商业平台，就必须受到平台的监管。以微信公众平台为例，微信每年清理的微信文章不计其数，除掉绝大部分造谣、抄袭的垃圾文章外，还有极小部分揭露社会黑暗的文章因为某些不可描述的原因而被删除。</li>
</ul>
<p>如果你也有同样的顾虑，可以尝试跟随我的脚步，搭建一个属于自己的个人博客。</p>
<h1 id="hexo">Hexo</h1>
<p>在开源社区各路大神的努力下，已经诞生了不少博客框架，我按照我个人的喜好，选择了『Hexo』，你也可以选择其他框架搭建。</p>
<p>『Hexo』官网对于这个框架的描述是：</p>
<blockquote>
<p>A fast, simple &amp; powerful blog framework</p>
<p>快速、简洁且高效的博客框架</p>
</blockquote>
<p>同时，还介绍了几个优点：</p>
<ul>
<li><strong>超快速度</strong>。Node.js 所带来的超快生成速度，让上百个页面在几秒内瞬间完成渲染。</li>
<li><strong>支持 Markdown</strong>。Hexo 支持 <a href="https://help.github.com/articles/github-flavored-markdown">GitHub Flavored Markdown</a> 的所有功能，甚至可以整合 Octopress 的大多数插件。</li>
<li><strong>一键部署</strong>。只需一条指令即可部署到 <a href="https://pages.github.com">GitHub Pages</a>, <a href="https://www.heroku.com/">Heroku</a> 或其他平台。</li>
<li><strong>插件和可扩展性</strong>。强大的 API 带来无限的可能，与数种模板引擎（EJS，Pug，Nunjucks）和工具（Babel，PostCSS，Less/Sass）轻易集成。</li>
</ul>
<p>尽管我认同『Hexo』的确是一个优秀的博客框架，但在这里我必须要泼一下冷水：</p>
<ul>
<li>网页的加载速度还依赖于实际网络情况。</li>
<li>不支持 Markdown 的博客框架算不上博客框架。</li>
<li>绝大多数的优秀博客框架都支持一键部署。</li>
<li>拥有插件扩展的博客框架才算优秀框架。</li>
</ul>
<p>总之，『Hexo』满足了优秀博客框架的条件，接下来我就带你利用『Hexo』一步一步搭建自己的博客。</p>
<h1 id="工具">工具</h1>
<h2 id="nodejs">Node.js</h2>
<p>上面也提到，『Hexo』是基于 Node.js 的，它是一个基于 Chrome V8 引擎 的 JavaScript 运行时，不懂没有关系，你只需要下载安装即可。</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1612968811233.png" alt="Node.js" loading="lazy"></figure>
<p>其实 Node.js 在之前『<a href="https://liarrdev.github.io/post/Set-up-React-Native-Development-Environment-on-Windows/">Windows 平台 React Native 开发环境搭建笔记</a>』中也有介绍过，如果感觉使用过程中网速不理想可以参考切换国内镜像源。</p>
<p>建议使用 LTS 版本。</p>
<h2 id="git">Git</h2>
<p>Git 也是我们程序员的老朋友了，它可以帮助我们拉取相关文件以及将博客部署到网上去，也只需要下载安装即可。</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1612968834183.png" alt="Git" loading="lazy"></figure>
<p>新版本的 Git 已经默认勾选「Run Git from Windows Command Prompt」了，也就是说可以自动添加到环境变量。</p>
<p>macOS 中安装 Git 则更为方便，直接在『Terminal』中输入 <code>git</code> 命令就会自动提示安装。</p>
<h1 id="环境">环境</h1>
<p>只需安装以上两个软件，我们就已经准备好工具啦，接下来在任意终端（你可以使用刚刚下载的『Git Bash』，或者系统自带的『CMD』、『Windows PowerShell』、『Terminal』等）输入以下命令：</p>
<pre><code class="language-Bash">➜   npm install -g hexo-cli
</code></pre>
<p>该命令会安装『Hexo』及其相关命令，等待一小会环境就安装完成啦。</p>
<h1 id="建站">建站</h1>
<p>你可能会觉得前面的准备工作太过简单，或许这正是『Hexo』的优点，但接下来才是我们的重点工作。</p>
<p>『Hexo』环境安装完成后，我们在终端执行：</p>
<pre><code class="language-Bash">➜   hexo init &lt;folder&gt;
</code></pre>
<p><code>&lt;folder&gt;</code> 是我们想要将整个博客环境存放的目录路径，这个命令会加载对应的文件到该文件夹下，你可能会好奇这些文件是从哪里来的，『Hexo』的 Github 项目中有一个叫「hexo-starter」的 Repo，所有的包文件都在其中。</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1612968887543.png" alt="hexo-starter" loading="lazy"></figure>
<p>执行完上述命令之后很可能会有相关错误，比如无法安装相应的包扩展文件等，我们可以进入刚刚生成的该文件夹把没有安装上的包再安装一遍：</p>
<pre><code class="language-Bash">➜   cd &lt;folder&gt;
➜   npm install
</code></pre>
<p>这样就把所需要的包和文件安装完成，指定文件夹的目录结构如下：</p>
<pre><code>.
├── _config.yml
├── package.json
├── scaffolds
├── source
|   ├── _drafts
|   └── _posts
└── themes
</code></pre>
<p>「_config.yml」是网站的配置信息，可以在此配置大部分的参数。</p>
<p>「package.json」是应用程序的信息，EJS、Stylus 和 Markdown renderer 皆已默认安装。</p>
<p>「scaffolds」是模版文件夹。当您新建文章时，『Hexo』会根据「scaffolds」来建立文件。『Hexo』的模板是指在新建的 Markdown 文件中默认填充的内容。</p>
<p>「source」是资源文件夹，存放用户资源的地方。除「_posts」文件夹之外，开头命名为 <code>_</code>（下划线）的文件、文件夹和隐藏的文件将会被忽略。Markdown 和 HTML 文件会被解析并放到「public」文件夹，而其他文件会被拷贝过去。</p>
<p>「themes」是主题文件夹，『Hexo』会根据主题来生成静态页面。</p>
<p>接下来就可以生成我们的网站页面了，在创建的项目文件夹根目录下，执行命令：</p>
<pre><code class="language-Bash">➜   hexo generate
</code></pre>
<p>因为单词比较长容易输错，『Hexo』支持用简写的方式执行命令，上面这条命令可以简写成：</p>
<pre><code class="language-Bash">➜   hexo g
</code></pre>
<p>如果你使用『Windows PowerShell』的话可能会遇到输出信息缺失的情况：</p>
<figure data-type="image" tabindex="4"><img src="https://LiarrDev.github.io/post-images/1612968986555.png" alt="Windows PowerShell 信息缺失" loading="lazy"></figure>
<p>不用太担心，这只是『Hexo』命令显示不兼容『Windows PowerShell』的缘故，所以我还是比较习惯用『Git Bash』，不过『Git Bash』省略了比较多的信息，你也可以使用『CMD』，它会显示完整的信息。无论你使用哪个终端，只要它没有报错，我们的命令都是正常执行的。</p>
<p>该命令执行完成后，可以发现目录下多了一个名为「public」的文件夹，该目录就是生成的网站。</p>
<p>这时候再执行命令：</p>
<pre><code class="language-Bash">➜   hexo server
</code></pre>
<p>或者简写：</p>
<pre><code class="language-Bash">➜   hexo s
</code></pre>
<p>在浏览器中访问网址：http://localhost:4000，就可以查看刚刚生成的网站。</p>
<figure data-type="image" tabindex="5"><img src="https://LiarrDev.github.io/post-images/1612969066699.jpeg" alt="" loading="lazy"></figure>
<p>我们的网站就建立起来了！</p>
<h1 id="写作">写作</h1>
<p>虽然网站搭建好了，但里面除了一个「Hello World」还是空空如也，既然作为我们的个人博客，那么就需要用自己的内容来把它充实起来。</p>
<p>执行以下命令来创建一篇新文章：</p>
<pre><code class="language-Bash">➜   hexo new [layout] &lt;title&gt;
</code></pre>
<p>你可以在命令中指定文章的布局（<code>layout</code>），默认为 <code>post</code>，可通过修改「_config.yml」中的 <code>default_layout</code> 参数来指定默认布局。</p>
<p><code>post</code> 相当于发布的文章，你也可以选择 <code>draft</code> 生成一个草稿，草稿页面不会被『Hexo』渲染生成但也可以预览其效果。</p>
<p>如果你担心命令太长太多你记不住，你可以把这个参数省略掉，记住如下写法即可：</p>
<pre><code class="language-Bash">➜   hexo new &quot;文章标题&quot;
</code></pre>
<p>执行完命令后你可以看到『Hexo』帮我们生成了一个与 <code>&lt;title&gt;</code> 名字相对应的 Markdown 文件，默认情况下 <code>&lt;title&gt;</code> 即文章的标题。</p>
<p>打开该文件，可以看到『Hexo』已经帮我们初始化了一些基本信息：</p>
<pre><code class="language-YAML">---
title: &lt;title&gt;
date: 2019-06-03 20:49:47
tags: 
---
</code></pre>
<p>文件最上方以 <code>---</code> 分隔的这一部分区域叫作「Front-matter」，用于指定个别文件的变量，简单来说就是这个文章的一些属性。</p>
<p>「Front-matter」通常使用 YAML 语法，当然你也可以使用 JSON 来编写，不过个人觉得虽然大部分程序员都比较熟悉 JSON 但在这里无疑使用 YAML 要更加方便易读，况且 YAML 学起来跟 JSON 一样无成本不是么。</p>
<p>以下是预先定义的参数，您可在模板中使用这些参数值并加以利用。</p>
<table>
<thead>
<tr>
<th style="text-align:center">参数</th>
<th style="text-align:center">描述</th>
<th style="text-align:center">默认值</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center"><code>layout</code></td>
<td style="text-align:center">布局</td>
<td style="text-align:center">「config.default_layout」中设置的默认值</td>
</tr>
<tr>
<td style="text-align:center"><code>title</code></td>
<td style="text-align:center">标题</td>
<td style="text-align:center">文章的文件名</td>
</tr>
<tr>
<td style="text-align:center"><code>date</code></td>
<td style="text-align:center">建立日期</td>
<td style="text-align:center">文件建立日期</td>
</tr>
<tr>
<td style="text-align:center"><code>updated</code></td>
<td style="text-align:center">更新日期</td>
<td style="text-align:center">文件更新日期</td>
</tr>
<tr>
<td style="text-align:center"><code>comments</code></td>
<td style="text-align:center">开启文章的评论功能</td>
<td style="text-align:center">true</td>
</tr>
<tr>
<td style="text-align:center"><code>tags</code></td>
<td style="text-align:center">标签（不适用于分页）</td>
<td style="text-align:center"></td>
</tr>
<tr>
<td style="text-align:center"><code>categories</code></td>
<td style="text-align:center">分类（不适用于分页）</td>
<td style="text-align:center"></td>
</tr>
<tr>
<td style="text-align:center"><code>permalink</code></td>
<td style="text-align:center">覆盖文章网址</td>
<td style="text-align:center"></td>
</tr>
</tbody>
</table>
<p>我一般还会使用 <code>subtitle</code> 指定副标题，使用 <code>cover</code> 指定文章封面图等，不过这并不是『Hexo』预先定义的参数，而是跟当前使用的主题相关，我使用这两个参数还是来源于在『微信公众平台』上写推文的习惯。</p>
<p>接下来只需要在「Front-matter」下方使用 Markdown 语法进行文章写作即可，任意文本编辑器都可以对其进行编辑，为了方便也可以使用专门的 Markdown 编辑器。</p>
<p>需要注意的是，『Hexo』中的 Markdown 语法使用的是 <a href="https://help.github.com/articles/github-flavored-markdown">GFM（GitHub Flavored Markdown）</a>而不是标准的 Markdown 语法，两者有点区别但总体区别不大，对于我日常使用来说，<a href="https://help.github.com/articles/github-flavored-markdown">GFM</a> 要相对严格一些（客观上来说并不是，因为 <a href="https://help.github.com/articles/github-flavored-markdown">GFM</a> 也有比标准 Markdown 自由的地方）。</p>
<p>写作完成后，再次运行上一小节生成网站以及启动服务器的两条命令：</p>
<pre><code class="language-Bash">➜   hexo g
➜   hexo s
</code></pre>
<p>访问 http://localhost:4000 即可查看刚刚生成的文章了。</p>
<p>这两条命令应当牢牢记住，因为在对博客做任何更新后都要用这两条命令来启动预览。</p>
<h1 id="配置">配置</h1>
<p>对于新手其实只需要了解几个常用的配置项即可，但如果想要定制更加个性的网站，就需要了解如何玩转配置文件了。</p>
<p>首先，配置文件指的是「_config.yml」，它可以分为网站配置文件和主题配置文件，分别存放于项目根目录以及对应主题目录。</p>
<p>正如它的文件名后缀所示，配置文件也是遵循 YAML 语法的，同样，凡是支持 YAML 的地方都是可以使用 JSON 来替代。</p>
<p>最主要的配置有几个地方，第一是网站基础配置。</p>
<p>比如你打开网站后你可以看到网站的标题是「Hexo」：</p>
<figure data-type="image" tabindex="6"><img src="https://LiarrDev.github.io/post-images/1612969261942.png" alt="Default Title" loading="lazy"></figure>
<p>我们的个人博客当然要自己起一个名字，所以可以修改：</p>
<pre><code class="language-YAML"># Site
title: &quot;Liarr's Studio&quot;
subtitle: 
description:
keywords:
author: Liarr
language:
timezone:
</code></pre>
<p><code>title</code> 就是网站的标题，我这里把它命名为「Liarr's Studio」，意为我的工作台。重新生成并重启服务器，就可以看到网站的标题改为了我定义的名字了。</p>
<figure data-type="image" tabindex="7"><img src="https://LiarrDev.github.io/post-images/1612969305806.png" alt="Customize Title" loading="lazy"></figure>
<p>第二个需要修改的是部署的配置。</p>
<p>上文虽然你看到的是你生成的网站，但这也仅限于本地查看，他人还无法在公网上访问你的博客，所以你还需要把它部署到服务器上，具体部署的步骤下文会详细说，在配置这里需要填写你部署的方式以及地址，如我这里填写的是：</p>
<pre><code class="language-YAML"># Deployment
deploy:
  type: git
  repo: https://github.com/LiarrDev/LiarrDev.github.io.git
</code></pre>
<p>也就是说，我使用 Git 的方式来部署，并且存放在『Github』对应的仓库中。<br>
第三个需要修改的是主题的配置。</p>
<p>你一定会认为默认的主题「Landscape」丑死了，我们可以通过修改主题来美化自己的网站，配置上只需要填写主题的名字即可，比如我这里填写：</p>
<pre><code class="language-YAML"># Extensions
theme: hexo-theme-clean-blog
</code></pre>
<p>就是使用这款名为「CleanBlog」的主题，具体设置主题的步骤下文也会有详细的介绍。</p>
<p>由于配置门槛较高，所以建议还是参考官方文档，官方文档上有更详细的介绍，且英文文档要比中文文档更加详细。</p>
<h1 id="部署">部署</h1>
<p>『Hexo』支持多种部署方式，比如 Git、Heroku、Netlify、Rsync 等，这里主要介绍流行的 Git 部署方式。</p>
<p>正如前文所提到，我的博客是部署在『Github』上的，没错，我使用的正是 <a href="https://pages.github.com">GitHub Pages</a>，这是『Github』给开发者建立一个私人页面，用来分享新颖的想法和自己写的代码。</p>
<p>不过由于『Github』的服务器在海外，我们在国内访问有时候会不稳定，再加上时不时会遇上 DNS 污染，对于国内的非专业用户，我个人更加建议选用国内的代码托管平台，比如『<a href="https://gitee.com">Gitee</a>』和『<a href="https://coding.net">CODING</a>』，部署流程和我在『Github』上部署基本没有太大差别。</p>
<p>注册的流程就不赘述了，直接讲部署流程。</p>
<p>首先得新建一个代码仓库，我们一般叫 Repo。</p>
<figure data-type="image" tabindex="8"><img src="https://LiarrDev.github.io/post-images/1612969578142.png" alt="创建 Repo" loading="lazy"></figure>
<p>创建的信息必须得严格按照标准来填写。</p>
<p>首先是「Repository name」，也就是仓库的名称，使用 <a href="https://pages.github.com">Github Pages</a> 必须按照 <code>&lt;Username&gt;.github.io</code> 的方式填写，<code>Username</code> 一般跟左边的 Owner 是相同的，就是你注册的时候填写的用户名。由于我已经创建好我的个人网站了，所以这里它会提示该仓库已存在。</p>
<p>如果你使用其他代码托管平台则参照对应平台的命名规范，国内的代码托管平台一般都有中文文档，可以轻松参考。</p>
<p>其他内容可以根据自己的需要进行改动，比如「Description」是对这个仓库的描述，它是一个选填项，所以你可以在以后对它进行修改，比如我的仓库描述为：</p>
<figure data-type="image" tabindex="9"><img src="https://LiarrDev.github.io/post-images/1612969560210.png" alt="Repo Description" loading="lazy"></figure>
<p>可以让别人直接通过点击跳转至我的博客主页。</p>
<p>对于仓库的权限一般不修改，即默认的「Public」即可。</p>
<p>『Github』之前对于「Private」私人仓库是要收费的，被 Microsoft 收购之后个人也可以免费创建私有仓库了，并且数量无限制。设立私有仓库的目的主要是使代码仅为内部人员使用防止被竞争对手拿到核心代码，而我们搭建个人博客这种基于开源项目上的代码，也没必要设为私有。</p>
<p>创建完成后，打开仓库，点击「Clone」菜单的的复制按钮把仓库的地址复制下来。</p>
<figure data-type="image" tabindex="10"><img src="https://LiarrDev.github.io/post-images/1612969661946.png" alt="Copy Repo Address" loading="lazy"></figure>
<p>填写到配置文件当中，如上文提到的部署配置所示。</p>
<pre><code class="language-YAML"># Deployment
deploy:
  type: git
  repo: https://github.com/LiarrDev/LiarrDev.github.io.git
</code></pre>
<p>这样，『Hexo』就知道应该部署到哪个仓库中去了。</p>
<p>写作完成并在本地预览无误后，就可以通过命令部署到『GitHub Pages』中去：</p>
<pre><code class="language-Bash">➜   hexo deploy
</code></pre>
<p>也可以简写：</p>
<pre><code class="language-Bash">➜   hexo d
</code></pre>
<p>这条命令同样非常重要，当你本地预览网站觉得没有问题后，通过这条命令就能将网站部署到线上。</p>
<p>然后你就可以在公网上访问你的个人博客网站了。</p>
<p>地址为：https://&lt;Username&gt;.github.io。</p>
<p>如果你初次执行命令后有关于 Github 方面的报错，也可以尝试通过把仓库 Clone 下来并把里面的文件复制到你的 Hexo 博客项目目录下进行解决。</p>
<p>假若有时候修改内容后网站依旧无法正常展示，可以尝试通过清除缓存的命令来解决：</p>
<pre><code class="language-Bash">➜   hexo clean
</code></pre>
<p>清除缓存后再部署就能够看到修改后的效果，如果依然不行，有可能是其他地方的问题，可以参考文档或相关教程一一排查。</p>
<h1 id="主题">主题</h1>
<p>默认的「Landscape」主题虽然比『CSDN』之类的广告流要好很多，甚至比很多国内你见到的博客都要好看，但依然无法满足我们对美观的追求，开源的『Hexo』有一个好处就是大家都可以为其打造主题。</p>
<p>截止至文章发表时，官网上登记的已经有 331 个主题，其他未被『Hexo』官方收录的主题应该也是不少的，所以你可以根据自己的喜好来选择合适的主题，当然，如果你能够自己制作一款主题供大家使用那就再好不过了。</p>
<p>打开『Hexo』官方的<a href="https://hexo.io/themes/">主题页</a>，这里展示了各种各样的主题，可以花些时间慢慢挑选。</p>
<figure data-type="image" tabindex="11"><img src="https://LiarrDev.github.io/post-images/1612969737354.png" alt="Hexo Themes" loading="lazy"></figure>
<p>鼠标移动到主题的封面图上，会有一只小眼睛，点进去可以对不同主题的效果进行预览。</p>
<p>其实大多都是开发者的个人博客，也刚好看看效果和内容。</p>
<p>选到合适的主题后，回到官方的主题页，点击该<strong>主题的名字</strong>（我觉得这种交互方式十分不好，因为用户更加习惯点击图片，而且鼠标移动到名字时仅鼠标手势改变了，名字也没有进行高亮或者动效提示），就可以进入到该主题的项目主页。</p>
<p>接着把对应的主题 Clone 至博客项目的「themes」目录下即可。</p>
<p>然后再在项目配置文件中如上文所述载入主题。</p>
<pre><code class="language-YAML"># Extensions
theme: hexo-theme-clean-blog
</code></pre>
<p>如我这里载入的就是名为「CleanBlog」的主题。</p>
<p>不同的主题也有不同的配置项，如这款「CleanBlog」主题我可以配置 Header：</p>
<pre><code class="language-YAML"># Header
menu:
  Home: /
  Archives: /archives
  Tags: /tags
  Categories: /categories
  Github:
    url: https://github.com/LiarrDev
    icon: github
</code></pre>
<p>首页的封面图我暂时还是使用其默认图片：</p>
<pre><code class="language-YAML"># URL of the Home page image
index_cover: http://www.codeblocq.com/assets/projects/hexo-theme-clean-blog/img/home-bg.jpg
</code></pre>
<p>上面可以看到，网站的 Favicon 默认是一个空白页的 ICON：</p>
<figure data-type="image" tabindex="12"><img src="https://LiarrDev.github.io/post-images/1612969795589.png" alt="Default Favicon" loading="lazy"></figure>
<p>这样看起来就会觉得很 Low，你看看各大网站，哪个没有自己专属的 LOGO？</p>
<figure data-type="image" tabindex="13"><img src="https://LiarrDev.github.io/post-images/1612969830922.png" alt="各大网站的 Favicon" loading="lazy"></figure>
<p>那我们也应该给自己的网站设定一个 Favicon，为了方便，我就直接把我个人公众号的头像拿过来了，先把它转成 ICO 格式，然后再存放在该主题目录下的 <code>/source/img/</code> 目录下，然后在主题配置中引入：</p>
<pre><code class="language-YAML"># set your own favicon
favicon: /img/favicon.ico
</code></pre>
<p>就可以把网站的 Favicon 改为我自己设计的 LOGO 了。</p>
<figure data-type="image" tabindex="14"><img src="https://LiarrDev.github.io/post-images/1612969875196.jpeg" alt="Customize Favicon" loading="lazy"></figure>
<p>另外，「CleanBlog」这款主题还支持加上自己的社交账号，这里我仅设定我的 Github 账号：</p>
<pre><code class="language-YAML"># Social Accounts
twitter_url:
twitter_handle:
facebook_url:
github_url: https://github.com/LiarrDev
gitlab_url:
linkedin_url:
mailto:
</code></pre>
<p>我的博客最终成品大致如下：</p>
<figure data-type="image" tabindex="15"><img src="https://LiarrDev.github.io/post-images/1612969916418.jpeg" alt="Blog Preview" loading="lazy"></figure>
<p>其实每一款主题都有不同的参数配置，具体根据选定的主题进行配置即可。</p>
<hr>
<p>至此，属于自己的个人博客就基本搭建完成了，有空也可以来我的技术博客中交流一下。</p>
<p>关于『Hexo』更详细的玩法可以参考官方文档。</p>
<p>在快餐式互联网时代，个人 Blog 实际上也越来越少见，大众娱乐化使得创作者更加愿意使用碎片化的记录方式，也几乎没有多少人愿意读长篇大论的文章，但无论是对写博客的作者，还是读博客的读者来说，都是一种沉淀。</p>
<hr>
<h1 id="参考内容">参考内容</h1>
<ul>
<li><a href="https://hexo.io">Hexo</a></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NestedScrollView 解决 Android 嵌套滚动问题]]></title>
        <id>https://LiarrDev.github.io/post/Solve-the-Android-Nested-Scrolling-Problem-with-NestedScrollView/</id>
        <link href="https://LiarrDev.github.io/post/Solve-the-Android-Nested-Scrolling-Problem-with-NestedScrollView/">
        </link>
        <updated>2020-12-13T16:00:00.000Z</updated>
        <content type="html"><![CDATA[<p>滚动是所有客户端都很常用的交互，毕竟屏幕就这么大，显示的内容往往非常多。</p>
<p>Android 开发中，新手常常会使用 <code>ScrollView</code> 来实现滚动的效果 —— Which is not wrong。</p>
<p>随着需求的增加，可能会需要做一个能滚动的列表，你马上就能反应过来，<code>RecyclerView</code> 或者 <code>ListView</code> —— Which is also correct。</p>
<p>需求再增加，需要做一个如同各电商平台一样，上半部分是某种固定形式的布局，下方是列表的可滚动的效果：</p>
<figure data-type="image" tabindex="1"><img src="https://LiarrDev.github.io/post-images/1607871945430.png" alt="UI 图" loading="lazy"></figure>
<p>新手的直觉是：<code>ScrollView</code> 内嵌套 <code>RecyclerView</code> 或者 <code>ListView</code> —— Which might be able to work —— But definitely not right。</p>
<p>我用 <code>ScrollView</code> 和 <code>RecyclerView</code> 简单写一下上面这个布局：</p>
<pre><code class="language-XML">&lt;ScrollView
    android:layout_width=&quot;match_parent&quot;
    android:layout_height=&quot;match_parent&quot;&gt;

    &lt;LinearLayout
        android:layout_width=&quot;match_parent&quot;
        android:layout_height=&quot;wrap_content&quot;
        android:orientation=&quot;vertical&quot;&gt;

        &lt;!-- 网格布局 --&gt;
        &lt;GridLayout
            android:layout_width=&quot;match_parent&quot;
            android:layout_height=&quot;wrap_content&quot;
            android:columnCount=&quot;3&quot;
            android:rowCount=&quot;2&quot;&gt;
            ...
        &lt;/GridLayout&gt;

        &lt;!-- 展示列表 --&gt;
        &lt;androidx.recyclerview.widget.RecyclerView
            android:layout_width=&quot;match_parent&quot;
            android:layout_height=&quot;wrap_content&quot; /&gt;
    &lt;/LinearLayout&gt;
&lt;/ScrollView&gt;
</code></pre>
<p>实际上会得到下面这种效果：</p>
<figure data-type="image" tabindex="2"><img src="https://LiarrDev.github.io/post-images/1562415419142.gif" alt="滚动冲突" loading="lazy"></figure>
<p>不难看出，<code>RecyclerView</code> 上方的网格布局在跟随 <code>ScrollView</code> 滚动，但 <code>RecyclerView</code> 却由于自身能够滚动的特性，只能在自己所处的位置滚动，而不能跟随 <code>ScrollView</code> 一起滚动。</p>
<p>这其实是一个滚动冲突的问题，解决办法也有很多，初学者遇到这个问题的时候往往是因为不知应如何描述问题而找不到答案。</p>
<p>其实<a href="https://developer.android.google.cn/reference/android/widget/ScrollView.html">关于 <code>ScrollView</code> 的 Android 官方文档</a>中有专门提到这个问题：</p>
<blockquote>
<p>Never add a <code>RecyclerView</code> or <code>ListView</code> to a scroll view. Doing so results in poor user interface performance and a poor user experience.</p>
</blockquote>
<p>同时，文档还提供了解决方案：</p>
<blockquote>
<p>For vertical scrolling, consider <code>NestedScrollView</code> instead of scroll view which offers greater user interface flexibility and support for the material design scrolling patterns.</p>
</blockquote>
<p><code>NestedScrollView</code> 是 Android 提供的专门解决嵌套滚动的容器，「Nested」其实就是嵌套的意思，它的使用方式和 <code>ScrollView</code> 基本没什么差别，但它能够支持嵌套滚动的父 <code>View</code> 或子 <code>View</code>。</p>
<p>因此，我们只要简单替换，就可以解决上面的问题：</p>
<pre><code class="language-XML">&lt;androidx.core.widget.NestedScrollView
    android:layout_width=&quot;match_parent&quot;
    android:layout_height=&quot;match_parent&quot;
    android:fillViewport=&quot;true&quot;&gt;

    &lt;LinearLayout
        android:layout_width=&quot;match_parent&quot;
        android:layout_height=&quot;wrap_content&quot;
        android:orientation=&quot;vertical&quot;&gt;

        &lt;!-- 网格布局 --&gt;
        &lt;GridLayout
            android:layout_width=&quot;match_parent&quot;
            android:layout_height=&quot;wrap_content&quot;
            android:columnCount=&quot;3&quot;
            android:rowCount=&quot;2&quot;&gt;
            ...
        &lt;/GridLayout&gt;

        &lt;!-- 展示列表 --&gt;
        &lt;androidx.recyclerview.widget.RecyclerView
            android:layout_width=&quot;match_parent&quot;
            android:layout_height=&quot;wrap_content&quot; /&gt;
    &lt;/LinearLayout&gt;
&lt;/androidx.core.widget.NestedScrollView&gt;
</code></pre>
<p>效果如下：</p>
<figure data-type="image" tabindex="3"><img src="https://LiarrDev.github.io/post-images/1562415718972.gif" alt="正常滚动" loading="lazy"></figure>
<p>虽然本文使用 <code>RecyclerView</code> 作为讲解，但如同文档中所说，这种方法同时还支持解决 <code>ListView</code> 的嵌套滚动问题。</p>
]]></content>
    </entry>
</feed>